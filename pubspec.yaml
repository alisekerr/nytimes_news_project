name: nytimes_news_project
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider:

  # Network
  dio: ^5.2.1+1
  pretty_dio_logger: ^1.3.1

  # Tool
  intl:

  cupertino_icons: ^1.0.2

dev_dependencies:
  #Test
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  http_mock_adapter: ^0.4.4

  #Lints
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
